---
apiVersion: helm.fluxcd.io/v1
kind: HelmRelease
metadata:
  name: nginx-deploy
  namespace: default
spec:
  releaseName: nginx-deploy
  chart:
    git: git@github.com:Naresh240/helm-chart-nginx.git
    ref: master
    path: charts/nginx
  values:
    replicaCount: 1
